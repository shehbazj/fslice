bitmap_alloc(bitmap b) {
    for (ix = 0; ix < 64; ix++) {
        // b is of size 64
        if (b[ix] != 0xff) {
        // return 1st bit index of 
        // 8 bits which is not set
        }
    }
    return -ENOSPC;
}
