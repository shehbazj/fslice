#include<stdio.h>
#include "sym.h"

int main()
{
	int i,j;
	int n = _mark_int(n);
	for(i = 0 ; i < n ; i++){
		for(j=0; j < i ; j++){
			;	
			if(i < j){
				;
			}else{
				;
			}
		}
	}
	return 0;
}
/* unsat
 * 0
 * 1
 * unsat
 * */
